@model List<Entradas>

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Entradas</title>
</head>
<body>

    <div class="header">
        <a href='@Url.Action("Index", "Home")'>
            <img src="/images/boca.jpg" alt="Boca Juniors" class="boca-image">
        </a>
        <h1>Conseguir Entradas</h1>
        <div class="nav-links">
            <a href='@Url.Action("Index", "Home")'>Inicio</a>
            <a href='@Url.Action("Partidos", "Home")'>Partidos</a>
            <a href='@Url.Action("Entradas", "Home")'>Entradas</a>
            <a href='@Url.Action("Noticias", "Home")'>Noticias</a>
        </div>
    </div>

    @{
        for(int i = 0; i<ViewBag.ListaEntradas.Count; i++){
            <p>@ViewBag.ListaEntradas[i].Asiento</p>
            <p>@ViewBag.ListaEntradas[i].Precio</p>
            <p>@ViewBag.ListaEntradas[i].EntradaID</p>
        }
    }

    <script>
        var body = document.querySelector('body');
        var colors = ['#FFD700', '#000033']; 
        var index = 0;

        function changeColor() {
            body.style.backgroundColor = colors[index];
            index = (index + 1) % colors.length;
            setTimeout(changeColor, 5000); 
        }

        changeColor();
    </script>

</body>
</html>